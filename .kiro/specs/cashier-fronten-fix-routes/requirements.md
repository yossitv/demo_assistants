# Requirements
- Rename all `casher_1` routes and folder to `cashier`.
- Root `/` should redirect to `/cashier`; `/cashier` should redirect to `/cashier/home`.
- Update every navigation/import reference from `/casher_1/*` to `/cashier/*`.
- No redirects left for old `/casher_1/*` paths.
